AddSpecialPaths = function(position_table, ls){
  return(add_special_paths(position_table = position_table, ls = ls))
}

SavePlot = function(plt, name){
  save_plot(plt, name)
}

MakeCircle = function(center = c(0,0), radius = 1, precision = 100){
  return(make_circle(center = center, radius = radius, precision = precision))
}

AddPointsToPlot = function(plot, ls){
  return(add_points_to_plot(plot, ls))
}

